# Todo App (Node.js + MongoDB)

A simple Todo API built with **Node.js**, **Express**, and **MongoDB**, following a **service → repo → controller** architecture.

---

## Features

- Create, read, update, delete todos
- RESTful API endpoints
- MongoDB integration
- Uses `.env` for sensitive info
- Structured with services, controllers, and repo layers

---
